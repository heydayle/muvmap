/**
 * Barrel export for shared hooks.
 * Per CLAUDE.md §1: single index.ts barrel export for shared/hooks.
 *
 * @module SharedHooks
 */

export {
  springPresets,
  pageTransition,
  listItemVariants,
} from './useAnimationPresets';

/**
 * Barrel export for location UI hooks.
 * Per CLAUDE.md §1: hooks have index.ts barrel export.
 *
 * @module LocationHooks
 */

export { useLocations, useLocationDetail, locationKeys } from './useLocations';
export { useLocationMutations } from './useLocationMutations';

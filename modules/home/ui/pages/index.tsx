'use client';

/**
 * Home module pages — main entry point.
 *
 * Per CLAUDE.md §1: `pages/index.tsx` is the canonical module page entry
 * exported to `app/page.tsx`.
 */
export { default } from './HomePage';
